# リポジトリエージェント

正本はrepository rootの`AGENTS.md`。本ファイルは互換entry pointとする。

- 適用する操作は `tasks/` を起点に記録する。
- 未実行の検証は完了として扱わない。
- 変更は以下の順で行う。
  1. 仕様・要件との整合確認
  2. `tasks/do` への受け入れ条件登録
  3. 実装
  4. 検証記録
  5. `reports/working` への完了報告

# ローカル制約

- 外部デプロイや本番変更は行わない。
- 破壊的変更は事前確認を必要とする。
